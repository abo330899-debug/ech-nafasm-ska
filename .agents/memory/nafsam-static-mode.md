---
name: Nafsam static (Cloudflare R2) mode
description: How Nafsam's static/R2 serving mode works and the password→token coupling that bites if forgotten.
---

# Nafsam static (R2) mode

In static mode the published Nafsam site serves everything from a public
Cloudflare R2 URL and does NOT call the API server. It is gated by three Vite
build-time vars: `VITE_STATIC_MODE=true`, `VITE_R2_BASE` (no trailing slash),
`VITE_AUTH_TOKENS`.

**Static mode must be branched in THREE places, not just media URLs:**
- `src/lib/r2.ts` — media/poster/image URL builders (R2 vs /api/private).
- `src/lib/auth.ts` — client-side fetchSession/login/logout via localStorage +
  `sha256(answer.trim().toLowerCase())` checked against `VITE_AUTH_TOKENS`.
- `src/hooks/usePrivateContent.ts` — loads `${R2_BASE}/content.json`; skips
  server revalidation.
If you only wire r2.ts, login and content still hit /api and static mode is broken.

**VITE_AUTH_TOKENS ↔ NAFSAM_PASSWORDS coupling (footgun):**
`VITE_AUTH_TOKENS` is a comma list of SHA-256 hashes of the login passwords,
each trimmed + lowercased + de-duplicated. It must be regenerated by hand any
time `NAFSAM_PASSWORDS` changes. They drift silently — a stale list was found
that locked out real passwords and accepted removed ones.
**Why:** login validation in static mode is fully client-side; the only source
of truth is whatever hashes were baked into the bundle at build time.
**How to apply:** after any password change, recompute hashes from the CURRENT
`NAFSAM_PASSWORDS` and update the production env var (and
`.env.cloudflare-pages`). Auth-token hashes are inherently public (embedded in
the downloadable JS bundle); short passwords are brute-forceable — a known,
user-accepted tradeoff.

**Deploy wiring:** `artifacts/nafsam/.replit-artifact/artifact.toml` inlines
`VITE_STATIC_MODE` + `VITE_R2_BASE` in the production build command;
`VITE_AUTH_TOKENS` is read from the production env var at build time (verified).
